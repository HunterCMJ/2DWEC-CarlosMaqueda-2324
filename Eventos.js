Elemento.addEventListener (nombreEvento, function)

if (document.addEventListener) {
    Element.addEventListener(nombreEvento, function) {
        
    }

} else if (document.attachEvent) {

    

} else {
    
}

if (document.addEventListener) {
    
    window.onload
}
